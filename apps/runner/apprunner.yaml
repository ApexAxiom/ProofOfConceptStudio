version: 1.0
runtime: nodejs18
build:
  commands:
    pre-build:
      - |
          cd ../../packages/shared
          npm install --include=dev --workspaces=false --install-links --no-audit --no-fund
          npm run build
      - npm install --include=dev --workspaces=false --install-links --no-audit --no-fund
      - node -e "console.log(require.resolve('@proof/shared/package.json'))"
      - node -e "console.log(require.resolve('@proof/shared'))"
    build:
      - npm run build
run:
  command: "npm start"
  network:
    port: 8080
