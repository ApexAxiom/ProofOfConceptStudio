version: 1.0
runtime: nodejs18
build:
  commands:
    - cd ../..
    - npm i -g pnpm@9.12.0
    - pnpm install
    - pnpm --filter @proof/runner... build
run:
  command: |
    cd ../..
    pnpm --filter @proof/runner start
port: 3002
