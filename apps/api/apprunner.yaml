version: 1.0
runtime: nodejs18
build:
  commands: |
    cd ../..
    npm i -g pnpm@9.12.0
    pnpm install
    pnpm --filter @proof/api... build
run:
  command: |
    cd ../..
    pnpm --filter @proof/api start
port: 3001
