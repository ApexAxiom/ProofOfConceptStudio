# @proof/shared

Shared TypeScript utilities for Proof of Concept Studio apps.

- Build outputs emit to `dist/` via `npm run build` (TypeScript project reference).
- Published/packed artifacts include `dist`, `package.json`, and this README for App Runner installs.
