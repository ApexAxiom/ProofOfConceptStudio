OPENAI_API_KEY=your-openai-key
OPENAI_MODEL=gpt-4o-mini
AWS_REGION=us-east-1
DDB_TABLE_NAME=CMHub
CRON_SECRET=change-me
ADMIN_TOKEN=change-me
API_BASE_URL=http://localhost:3001
RUNNER_BASE_URL=http://localhost:3002
WEB_PORT=3000
API_PORT=3001
RUNNER_PORT=3002
PORT=3000
DDB_ENDPOINT=
CORS_ORIGINS=
CHAT_RATE_LIMIT_RPM=
CHAT_RATE_LIMIT_BURST=
CHAT_MAX_CLAIMS=6
EVIDENCE_SIMILARITY_THRESHOLD=0.14
EVIDENCE_AUTO_MATCH_THRESHOLD=0.2
BING_IMAGE_KEY=
BING_IMAGE_ENDPOINT=https://api.bing.microsoft.com/v7.0/images/search

# AWS Secrets Manager Configuration (optional)
# When AWS_SECRET_NAME is set, the application will fetch secrets from AWS Secrets Manager
# at startup. Secrets from AWS are merged with environment variables, with env vars taking precedence.
# The secret should contain a JSON object with keys matching the env vars above (e.g., OPENAI_API_KEY, CRON_SECRET, etc.)
AWS_SECRET_NAME=
# Cache TTL for secrets in milliseconds (default: 300000 = 5 minutes)
SECRETS_CACHE_TTL_MS=300000
