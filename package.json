{
  "name": "proof-of-concept-studio",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "corepack pnpm -r --parallel dev --filter web --filter api --filter runner",
    "lint": "corepack pnpm -r lint",
    "build": "corepack pnpm -r build",
    "run:apac": "corepack pnpm --filter runner run cron:apac",
    "run:international": "corepack pnpm --filter runner run cron:international",
    "run-briefs": "corepack pnpm --filter runner run run-briefs",
    "coverage:report": "corepack pnpm --filter runner run coverage:report",
    "smoke:core": "corepack pnpm --filter api run latest-selection:smoke && corepack pnpm --filter runner run fallback:smoke && corepack pnpm --filter web run executive-dashboard:smoke"
  }
}
